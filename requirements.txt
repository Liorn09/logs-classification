# Core ML
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# Config
pyyaml>=6.0

# Excel support (required for data.xlsx → log_entries.csv conversion)
openpyxl>=3.1.0

# LLM summarizer (optional - only needed if summarizer.type=llm)
google-genai>=1.0.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Dev / testing
pytest>=7.4.0
